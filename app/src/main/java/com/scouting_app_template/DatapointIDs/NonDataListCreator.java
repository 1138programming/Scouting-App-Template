package com.scouting_app_template.DatapointIDs;
/*


public class NonDataListCreator {

    public static void main(String[] args) {
        printList("""



        """);
    }

    public static void printList(String nonDataList) {
        nonDataList = nonDataList.replaceAll("\t", "");
        nonDataList = nonDataList.replaceAll("\n", "");
        String[] nonDataArray = nonDataList.split(",");
        for(int i = 0; i < nonDataArray.length; i++) {
            String temp = "nonDataIDs.put(NonDataEnum." + nonDataArray[i] + ", -" + (i+1) + ");";
            System.out.println(temp);
        }
    }
}


*/